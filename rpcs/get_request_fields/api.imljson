{
    "url": "/api/v1",
    "method": "POST",
    "body": {
        "method": "fields.list",
        "params": {
            "formId": "{{parameters.formId}}"
        }
    },
    "response": {
        "output": "{{buildRequestFields(body.data.items)}}"
    }
}
