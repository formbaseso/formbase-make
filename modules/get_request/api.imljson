{
    "url": "/api/v1",
    "method": "POST",
    "body": {
        "method": "requests.get",
        "params": {
            "requestId": "{{parameters.requestId}}"
        }
    },
    "response": {
        "output": "{{body.data}}"
    }
}
