{
    "url": "/api/v1",
    "method": "POST",
    "body": {
        "method": "webhooks.create",
        "params": {
            "formId": "{{parameters.formId}}",
            "targetUrl": "{{webhook.url}}",
            "provider": "make",
            "eventType": "{{parameters.eventType}}",
            "idleWindow": "{{if(parameters.eventType = 'submission_abandoned', parameters.idleWindow, undefined)}}"
        }
    },
    "response": {
        "data": {
            "subscriptionId": "{{body.data.subscriptionId}}"
        }
    }
}
